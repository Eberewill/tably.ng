{
  "name": "tably",
  "private": true,
  "packageManager": "pnpm@11.8.0",
  "scripts": {
    "dev": "corepack pnpm --parallel --filter './apps/*' dev",
    "build": "corepack pnpm -r --if-present build",
    "typecheck": "corepack pnpm -r typecheck",
    "test": "corepack pnpm design:check && corepack pnpm -r test",
    "design:check": "node scripts/check-design-system.mjs",
    "format": "prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "turbo": "^2.5.6",
    "typescript": "^5.9.2"
  }
}
